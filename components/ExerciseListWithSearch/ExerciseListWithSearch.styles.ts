import styled from 'styled-components';

const StyledExerciseListWithSearch = styled.div``;

const StyledExerciseListSearch = styled.div`
  margin-bottom: ${({ theme }) => theme.spacing.base};
`;

export { StyledExerciseListWithSearch, StyledExerciseListSearch };
